# orbit/control — Dev 2 (console defaults)

Console `HumanGateway` + `ActivitySink` implementations so any layer can run
without the web UI. Dev 3's `orbit/ui/` replaces these at runtime. Keep both
implementing the Protocols in `orbit/contracts.py`. Owned by **Dev 2**.
